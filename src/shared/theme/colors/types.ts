export interface IDarkColors {
  text: {
    primary: string;
    white: string;
    button: {
      secondary: string;
    };
  };
  background: {
    primary: string;
    secondary: string;
  };
}

type SLA = () => number;
