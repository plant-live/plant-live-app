import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1e88e5',
      light: '#6ab7ff',
      dark: '#005cb2'
    },
    secondary: {
      main: '#e53935',
      light: '#ff6f60',
      dark: '#ab000d'
    }

    // outras cores personalizadas, se necessário
  }
});
